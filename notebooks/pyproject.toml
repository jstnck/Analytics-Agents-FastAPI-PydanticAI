[project]
name = "analytics-notebooks"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "marimo[sql]>=0.10.0",
    "duckdb>=1.1.0",
    "httpx>=0.28.0",
    "polars>=1.17.0",
    "pyarrow>=18.0.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
